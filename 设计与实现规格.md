# 《星渊突击》设计与实现规格

> 本文件记录游戏的完整设计规格与实现约束，作为后续开发的依据。修改功能前请先确认此处的既有约定。

## 一、项目信息

- GitHub 远程仓库：`https://github.com/fklnk/plane-game`
- 当前版本：`v0.6.5`
- 技术栈：TypeScript、Phaser 3、Vite、Canvas/WebGL、本地 `localStorage`
- 本地开发地址：`http://localhost:5173/`
- 主要代码：
  - `src/main.ts`：界面、战斗、流派、敌机、Boss、技能和游戏流程
  - `src/game-logic.ts`：存档结构、每日登录、时间/奖励/生命值工具
  - `src/boss-campaign.ts`：九场 Boss 战流程、难度与敌军配置
  - `src/styles.css`：全部 UI 样式
  - `tests/game-logic.test.ts`：单元测试

## 二、开发原则

1. 新需求一律基于当前代码做增量修改，不重建项目、不推翻现有玩法。
2. 若新需求与已实现功能重复，先核对现有实现，跳过重复部分。
3. 保留科技感、沉浸式驾驶舱、大地图、粒子反馈、Boss CG 和完整页面流程。
4. 每次修改后运行 TypeScript 检查、单元测试与生产构建，并做浏览器实测。
5. 保持存档兼容：玩家刷新、关闭页面或重新开局后不得丢失仓库代币与永久进度。

## 三、当前完整页面流程

`精简首页 → 选择战机 → 选择流派 → 选择模式/难度/双人玩法 → 战斗 → Boss/升级 → 最终结算`

首页已经改为简洁版本，包含：

- 当前战机展示
- 开始游戏
- 紧凑型每日登录奖励
- 机库商店
- 勋章展馆
- 设置
- 关于
- 仓库代币、无尽纪录和成就数量

## 四、现有模式

### 普通战役

- 普通战役包含五段独立清兵：第一 Boss 前、三次黑影追逐逃逸后，以及三 Boss 同场战之后。每段按进入航段后的累计分数触发下一场正式战斗，不得改回固定击杀数，小兵不额外增加九战编号。
- 清兵航段直接开始和直接结束，不显示全屏“开始清兵/清兵完成”面板。五段目标由 `CAMPAIGN_CLEAR_SCORE_TABLE`（`src/boss-campaign.ts`）按区域难度写死，不再用线性公式；噩梦（区域 5）为设计基准 `30000 / 52500 / 80000 / 100000 / 120000`，普通（区域 3）为 `24000 / 42000 / 64000 / 80000 / 96000`。段间增量不会随阶段推进而缩小。自然刷怪间隔额外乘 `1.1`，升级经验需求乘 `1.1` 后四舍五入为整数。
- 上述分数只能保留在内部逻辑、测试和 `?debug=1` 快照中。正式菜单、HUD、击杀 Banner、阶段开始/结束提示不得出现目标、剩余分数、`CLEAR x/5` 或阈值文字；普通战役 Boss 提示也不显示第几战，只在真正出现时识别当前威胁。
- 每段清兵在内部进度 `18% / 42% / 68% / 90%` 触发四次无数值神秘反馈：`+6` 终极能量、星核掉落、临时技能残片并 `+10` 终极能量、回复 `6%` 最大生命并 `+16` 终极能量。文本从三组回波文案轮换，禁止显示百分比。
- 连击奖励节点为 `8 / 20 / 40`：基础代币分别 `3 / 7 / 14` 后再加当前难度等级，终极能量分别增加 `5 / 10 / 18`。该机制只奖励连击，不参与 Boss 触发。
- 九战主线不得再加入点击继续式剧情弹窗。原始 Boss 死亡后黑影直接夺取其全部能力与强化并进入追逐；达到逃逸线后黑影只向上淡出隐退，并实际掉落对应 Boss 强化拾取物。玩家回收强化后直接进入下一清兵航段或下一场战斗。
- Boss 战流程：
  1. 第一 Boss
  2. 第一次追逐黑影
  3. 第二 Boss
  4. 第二次追逐黑影
  5. 第三 Boss
  6. 第三次追逐黑影
  7. 三 Boss 同场战
  8. 黑影最终战
  9. 黑暗魔神飞机
- 最终 Boss 击败后战役结束，奖励足以解锁商店剩余内容。
- 第三次追逐使用尚未完全消化第三股力量的金紫尖棱形态；三 Boss 同场和第五段清兵完成后，第八战才使用独立完全体黑影素材。
- 完全体黑影与最终 Boss 会周期召唤小兵；完全体剩余 `1/8` 生命时爆炸，清场、封锁玩家最大生命至 `3000`，并将所有回血机制与技能减半后显露最终真身。

### 深空无尽

- 小兵和 Boss 无限循环。
- 每击败一次 Boss，当前对局代币立即存入仓库。
- Boss 后的小兵阶段会明显延长，并随 Boss 轮数继续增长。

### 九战 Boss 战役

- 不自然生成小兵，固定完成九场后结束，不再无限重生。
- 顺序为：泰坦、第一次黑影追逐、镜像、第二次追逐、篡夺者、第三次追逐、三不完全体同屏、完全体黑影、最终真身。
- 第二、第三名 Boss 相对第一名为 `133.3%`、`150%`；三次追逐黑影为 `116.7%`、`141.7%`、`158.3%`，并逐次继承被夺取的招式。
- 三不完全体的普通与精英形态均保留各自完整模板的 `2/3` 数值倍率；每只拥有顶部共享总量 `33.33%` 的独立生命，必须分别打空，死亡顺序不受限制。
- 原始 Boss 死后玩家当场选择一个主动权柄，黑影只夺走该 Boss 的专属被动核心；打退黑影后再从该 Boss 的三项独有被动中选一项。
- 黑影前三次追逐使用三张逐次演化素材：泰坦暗橙裂隙、镜像青蓝镜片鳍、篡夺金紫权限刻痕与尖棱依次累积；第三形态仍表示尚未完全消化。三 Boss 同场及第五段清兵后，第八战改用稳定融合三种力量的独立完全体图，第九战使用独立最终真身图。

## 五、敌军、难度和 Boss

- 难度为普通、困难、噩梦。
- 普通：敌人均为普通形态。
- 困难：约 50% 精英敌军。
- 噩梦：约 75% 精英，约四分之一单位带突变能力，Boss 也有概率突变。
- 精英与突变为两次独立掷骰：突变不要求单位先成为精英，普通小兵也能突变，精英可以叠加突变。
- Boss 召唤的指定兵种同样参与精英与突变掷骰，不得再用 `forcedType` 屏蔽。
- 突变必须严格同阶：小兵只能抽取小兵突变池，Boss 只能抽取 Boss 模板池且排除自身；不得把 Boss 招式发给小兵或把小兵能力发给 Boss。
- 所有 Boss（含三次黑影追逐、完全体黑影与最终真身）都可以精英突变，攻击循环不得因 Boss 专属更新函数提前返回而跳过继承招式。
- 已有侦察机、拦截机、突击机、压制机、布雷机、炮艇、轰炸机、送死运输机等兵种。
- 所有主要敌机均有图片和精英形态。
- Boss 包含裂渊泰坦、镜像猎手、技能篡夺者、力量掠夺者黑影、黑暗魔神飞机。
- Boss 具有随机弹幕、追踪攻击、延迟爆炸、冲撞、召唤、技能封锁、能力复制、黑暗持续伤害和阶段转换。

## 六、战机与流派

战机：默认、爆破、闪电、守护。

流派：力量、敏捷、防御、吸血、吞噬、撞击。

### 已确定的重要数值规则

- 所有强化攻击的额外收益统一按原数值的三分之二计算。
- 全部流派的基础生命、攻击、速度、射速、暴击、拾取与回血提高 `1/6`；受到伤害和爆炸伤害倍率降低 `1/6`。技能冷却已回调为各流派原始倍率，体型大小是唯一不参与统一强化的基础字段。
- 非撞击流派 `1/2/3` 主动技能基础伤害已经减半。
- 所有生命值在状态和界面中四舍五入为整数，不显示小数。
- 其他玩家可见数值统一四舍五入，整数不显示小数位，非整数最多保留一位小数；禁止直接展示 JavaScript 浮点精度尾数。
- 敏捷流派每累计获得 100 最大生命，恢复当前已损生命的 9.3%。
- 敏捷流派的暴击率按每 1% 转换为 2% 攻击力，暴击效果按每 1% 转换为 0.3% 移速；攻击收益仍按全局三分之二规则结算。
- 敏捷流派影步突刺：位移强制使用当前等级上限（地图竖直长度的 40%/50%/60%，即 512/640/768），**不得读取鼠标位置**；方向由方向键或 WASD 决定，无按键时沿当前速度方向，静止时朝正上方。固定 400ms 完成，突进期间无敌，按线段扫掠判定命中，途经敌人全部受击；分级扫掠宽度 56/68/80，伤害 110%/140%/170%，冷却 30/25/20 秒。每命中回复 2% 最大生命，单次最多 5 次。
- 敏捷流派影分身：数量上限 4，攻击上限为本体 100%，血量上限为本体 40%，三项达上限后继续升级只缩短召唤间隔。分级为数量 1/2/3/4、血量 1 点/10%/25%/40%、攻击 25%/50%/75%/100%、间隔 30/25/20/15 秒，每次只补充 1 个。
- 影分身站位必须与本体保持同一水平线，目标固定为当前绝对血量最少的敌人；血量按本体当前最大生命动态换算并在变化时按百分比迁移；撞击按敌人真实伤害扣血，不得无视血量直接消散，也不得添加免伤或减免。
- 力量流派龙息三级参数固定为：长度 420/560/700、末端宽度 260/360/480、持续 700/800/900ms、每伤害帧 30/44/60、冷却 13/12/11 秒。真实判定必须使用从机头向上展开的锥形，不得退回横向椭圆或短距离矩形。
- 六流派实体语言必须区分：力量三层龙息、敏捷紫电相位残影、防御星盾反刺、吸血活体触手、吞噬深渊巨口与獠牙、撞击装甲波与碎片；常驻引擎拖尾也必须随流派变化。
- 撞击流派接触普通敌人的冷却约 300ms，接触 Boss 约 325ms，受伤保护约 500ms。
- 撞击流派每 5 秒恢复 5.8% 最大生命。
- 撞毁小兵增加该单位最大生命的 5%；撞毁 Boss 固定增加 300 最大生命。
- 撞击流派本局每累计新增 500 最大生命，当前攻击力提高 20%；该倍率作用于普通武装、小兵撞击、Boss 基础百分比撞击和破阵冲刺。
- 撞击流派最大生命超过 1000 时，受到的 Boss 直接伤害、百分比伤害和黑暗持续伤害降低四分之一。
- 所有流派最大生命每跨过 1000 点，小兵基础伤害提高 20%，且小兵每次有效伤害至少为玩家最大生命的 5%；该规则不作用于 Boss、友军和环境伤害。

## 七、撞击流派的最新硬性边界

代码内部仍使用历史 ID `wheelchair`，用户界面必须只显示“撞击流派”，不要再显示“轮椅流派”。

1. 撞击流派永久关闭基础机炮。
2. 按空格不能生成 `cannon` 普通子弹。
3. 升级池中不能出现脉冲机炮和无人机。
4. 撞击流派不能取得会发射普通机炮弹的镜像僚机；Boss 镜像权柄已经转换为特殊导弹。
5. 撞击流派可以取得普通流派的特殊武装：
   - 极光贯穿（内部 ID `laser`）
   - 追踪导弹（`missile`）
   - 电弧链（`arc`）
   - 旋刃力场（`blade`）
6. 临时棱镜和奇点能力在撞击流派下会转换成极光或导弹，不能产生普通子弹。
7. 撞击流派拥有专属升级：
   - 磁轨破城撞角 `ram_mass`
   - 矢量冲锋引擎 `ram_drive`
   - 动能偏转装甲 `ram_armor`
   - 撞击再生核心 `ram_regen`
   - 残骸吞噬器 `ram_salvage`
   - 动能震荡波 `ram_shockwave`
   - 战场回收磁场 `ram_magnet`
8. `E` 为破阵冲刺；撞击流派的专属主动技能固定为 `1` 破阵冲角、`2` 反应装甲、`3` 堡垒姿态；`Q`、后续支援、`G` 流派技能和 `V` 首领权柄仍然可用。
   - `1` 破阵冲角：9 秒冷却，向移动方向突进 380，首个目标受到 3 倍撞击伤害并被高速击飞；突进后 700ms 承伤降至 30%。
   - `2` 反应装甲：20 秒冷却，持续 4 秒，承伤降至 35%；被阻挡伤害蓄入装甲，下次舰体撞击释放为 3 倍伤害，并按释放伤害的 30% 回血。
   - `3` 堡垒姿态：24 秒冷却，持续 5 秒，体型 ×1.5、承伤降至 50%、移速 ×0.8；接触冷却缩短并强化台球击飞及继承震击波。
   - 多个主动减伤同时存在时只取最强值，不做乘算叠加。

## 八、商店、代币、存档和每日登录

- 商店可以升级永久生命、火力、引擎、装甲、回收、紧急修复、重抽等属性。
- 皮肤商店只展示成就皮肤，不再出售代币换色皮肤；代币用于永久升级，来源包括击杀、Boss、飞行拾取和模式奖励。
- 皮肤稀有度分为普通、稀有、史诗、神话、传说；五种终局失败结局各绑定一款唯一皮肤，Boss 专属九战模式完成后另授传说皮肤「九渊弑神」。
- 皮肤品质必须改变机体外观层级：普通基础机体、稀有双侧翼片、史诗双层装甲与节点、神话翼冠与独立核心、传说九重冠饰与完整外装。皮肤弹道只能使用小尺寸低亮度色标，不得遮挡敌弹；全部流派专属技能与首领权柄必须使用可辨识的独立启动轮廓。
- 导弹命中视觉使用闪光、冲击波、碎片与灼痕分层表现；泰坦、镜像、篡夺者、完全体黑影、黑暗魔神的招牌技能分别使用王冠、镜面、权限栅格、爪痕、日蚀预兆。
- 九战前三位 Boss 击破后必须立刻提供主动权柄三选一，当前 Boss 本源技能必出并允许替换当前 V 技能。随后黑影夺走的是该 Boss 的专属被动核心；只有击退对应追逐战黑影后才生成掉落，拾取时从三项该 Boss 独有被动中选择一项。
- 生成的动态特效图必须参与真实攻击阶段，并按判定范围设置显示宽高：车道、爪刀和光束铺满整条判定带，圆形场覆盖真实半径，冻结、封锁与全屏弹幕覆盖其实际作用区域；不得只在技能中心显示小图标。大范围图片必须位于敌弹下层并降低内部透明度，同时使用不参与缩放的高亮彩色外框、白色内沿和范围文字明确标出真实边界。
- 范围伤害代码和特效必须读取同一组宽高/半径：有范围伤害的位置必须有对应实体与边界，边界内外不得和伤害判定不一致。纯弹幕技能由实际子弹实体表达伤害，不得额外铺设没有持续伤害的整屏危险底图。Boss 施法核心使用不透明装甲环、反应堆和阵营构件，范围边缘使用实体角标、锚点或警示柱补足重量感。
- 战场代币与经验拾取已换成金色机械外圈、青蓝星核的透明 PNG 专属图标，不再使用容易被误认成回血道具的绿色十字。
- 怪物和 Boss 会继承玩家永久加点的一部分成长。
- 存档包含仓库代币、战机、流派、皮肤、升级、纪录、成就、模式、难度、双人配置和每日登录。
- 对局中未结算代币在刷新、关闭、返回菜单和重新开始时会自动归档，防止丢失。
- 每日登录为七日递增奖励：80、110、140、170、200、230、260；漏签后从第一天重算，同一天不能重复领取。

## 九、双人模式

- 支持单人、双人合作、双人积分竞技。
- P1 使用 WASD，P2 使用方向键，并有独立技能键。
- 双人模式允许互相爆破，包括子弹、激光、导弹、奇点和 EMP 等友军伤害。
- 双人 UI 会显示双方生命和分数。

## 十、性能与画面现状

- 默认启用高性能模式，优先高性能 GPU。
- Phaser 使用像素对齐并保留抗锯齿。
- 星空和粒子数量已适度降低，但保留光环、爆炸、震动和 Boss CG。
- 引擎尾焰使用对象池复用，不再持续创建并销毁大量对象。
- 最近目标搜索改为单次线性扫描，避免每帧排序。
- 已删除首页无用的下载入口、冗余任务介绍、七格签到轨道和对应 CSS。
- 不能为了性能完全删除击中、受击、爆炸和 Boss 出场反馈。

## 十一、验证命令

在项目目录运行：

```powershell
npx tsc --noEmit
npm test -- --run
npm run build
npm run verify:release
```

生产浏览器验收：

```powershell
$env:QA_BASE_URL='http://127.0.0.1:4185'
node work/qa/mode-save-daily-check.cjs
node work/qa/nine-boss-campaign-check.cjs
```

最近一次结果：

- TypeScript 检查通过（已开启 `noUnusedLocals` 与 `noUnusedParameters`）
- 29 项单元测试通过
- Vite 生产构建通过
- ZIP 列表、CRC 和解压验证通过
- E 盘版本浏览器自动化通过
- 已验证三模式、每日登录、无尽代币入库、双人互伤、刷新保护、最终 Boss、生命整数化和撞击流派武器边界
- 已验证敏捷流派 G 键突刺的射程截断、400ms 完成、整路径扫掠命中和连续两次发动
- 已验证影分身的数量/攻击/血量三项上限、动态血量迁移和按真实伤害扣血
- 已验证五种结局皮肤映射、Boss 模式传说皮肤解锁、Boss/小兵同阶突变候选池与自身模板排除
- 已验证 BOM 自动清除插件在文件保存时生效（注入 5 个后自动归零）

## 十二、G 键的流派分派边界

`G` 键为各流派共用键位，但技能逻辑完全独立，不允许再出现 fallthrough：

| 流派 | G 键技能 | 未满足条件时 |
| --- | --- | --- |
| 力量 | 龙息喷火 | 提示需要先取得「龙息喷火」强化 |
| 敏捷 | 影步突刺 | 提示需要先取得「影步突刺」强化 |
| 撞击 | 全速冲锋 | 提示冷却或被封锁 |
| 防御、吸血、吞噬 | 无主动技能（专属强化为被动） | 明确提示无需 G 键触发 |

敏捷流派未取得突刺强化时不得落入撞击流派分支，否则会错误提示「全速冲锋仅属于撞击流派」。

首领主动权柄只允许由 `V` 键释放，同时只能装备一个；当前 Boss 的本源主动必须进入当场三选一。黑影追逐掉落的是上一只 Boss 独有的专属被动三选一，被动可累计但一次只能选择一项且不占 V 键。玩家版直接伤害与复制伤害统一为 Boss 原版的 35%，基础冷却统一为 44 秒。绝对零度属于 Boss 技能池，冻结 5 秒。

冻结不得只把速度设为零：必须锁定敌机、Boss、黑暗飞机、克隆体及敌弹坐标，暂停绑定 tween，并在解冻时恢复 tween、原速度，同时补偿 `born`、`next*`、`*At`、`*Until` 与 Boss 运动相位。达到 Boss 阈值或进入 Boss CG 时必须先清空残余小兵、敌弹和玩家弹。

三神共战的三颗核心每次从对象池取出后必须显式恢复专属纹理、`active/visible`、`alpha=1`、普通混合模式、角度和 tint；专属纹理缺失时回退 `bossCore`，不得生成不可见核心。每颗核心拥有独立生命与独立血条，最大值固定为顶部共享总量的 33.33%；攻击只扣命中目标的独立生命，顶部共享条始终等于三条剩余生命之和。任一核心归零后立即炸毁、禁用碰撞并停止所有后续攻击，其余存活核心保持可见、可命中、可攻击，更新循环禁止重新启用死亡核心。

## 十三、BOM 自动化约束

`src/main.ts` 曾因编辑器反复保存累积多达 21 个 `U+FEFF`，在 IDE 首行显示为一串白框问号。现有三层防护，不要移除：

1. `.editorconfig` 声明 `charset = utf-8`，从源头阻止写入 BOM。
2. `npm run build` 与 `npm test` 前置 `node scripts/check-bom.mjs --fix` 自动修复。
3. 不要在 Vite 插件里监听文件变更并回写源文件：编辑器保存尚未完成时读取会拿到半截内容，覆盖回磁盘将导致源文件截断。

手动命令：`npm run check:bom` 检查，`npm run fix:bom` 清除。
